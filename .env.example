# Copy this file to .env.local and paste your key after the = sign.
# .env.local is git-ignored, so the key never reaches GitHub.
#
# Used only by `npm run dev`. The deployed site ignores it on purpose and
# always asks visitors for their own key (see lib/api-key.ts).
ANTHROPIC_API_KEY=""
